# 13.2 — Ansible — Introduction, Architecture (Agentless, SSH-Based)

> **One Line Answer:** Ansible is an **open-source, agentless configuration management and automation tool** that connects to your servers over **SSH** (no software to install on them), reads simple **YAML files** (called Playbooks) that describe what the servers should look like, and then makes it happen — it's the most popular CM tool because it's ridiculously simple to learn and incredibly powerful at scale.

---

## Table of Contents

- [Why Ansible? — The Problem It Solves](#why-ansible--the-problem-it-solves)
- [What is Ansible? — The Basics](#what-is-ansible--the-basics)
- [Ansible Architecture — How It Works Under the Hood](#ansible-architecture--how-it-works-under-the-hood)
- [The "Agentless" Architecture — Why It's a Big Deal](#the-agentless-architecture--why-its-a-big-deal)
- [SSH-Based Communication — How Ansible Connects](#ssh-based-communication--how-ansible-connects)
- [Ansible Execution Flow — What Happens When You Run a Playbook](#ansible-execution-flow--what-happens-when-you-run-a-playbook)
- [Ansible Project Structure — How Files Are Organized](#ansible-project-structure--how-files-are-organized)
- [Ansible vs Other CM Tools — Why Ansible Wins](#ansible-vs-other-cm-tools--why-ansible-wins)
- [Key Terminology Recap](#key-terminology-recap)
- [Summary](#summary)
- [What's Next?](#whats-next)

---

## 🤔 Why Ansible? — The Problem It Solves

Let's say you've been managing servers for a while now. Here's what your life looks like:

```
Your current workflow:
    1. SSH into server-1
    2. Run 15 commands to set it up
    3. SSH into server-2
    4. Run the same 15 commands
    5. Repeat for server-3, server-4, ... server-50
    6. Realize you forgot to run command #7 on server-12
    7. SSH back into server-12
    8. Fix it
    9. Write a bash script to automate this
    10. Bash script works on Ubuntu but breaks on CentOS
    11. Add a bunch of if/else statements
    12. Script becomes 500 lines and unmaintainable
    13. Someone asks you to also configure the database servers... differently
    14. You cry a little 😭
```

**Ansible says: "Give me a YAML file describing what you want, and I'll handle the rest."**

```
Your life WITH Ansible:
    1. Write a simple YAML file (playbook)
    2. List your servers in an inventory file
    3. Run: ansible-playbook setup.yml
    4. All 50 servers are configured in minutes
    5. Need to add 20 more servers? Just add them to inventory
    6. Run the same playbook again
    7. Done ✅
    8. Go grab coffee ☕
```

---

## 🏗️ What is Ansible? — The Basics

**Ansible** is:

- An **open-source** automation tool
- Created by **Michael DeHaan** in 2012
- Acquired by **Red Hat** in 2015 (Red Hat is owned by IBM)
- Written in **Python**
- Uses **YAML** for configuration (human-readable, easy to learn)
- **Agentless** — nothing to install on managed servers
- Connects via **SSH** (Linux) or **WinRM** (Windows)
- **Idempotent** — safe to run multiple times

### What Can Ansible Do?

Ansible isn't just for configuration management. It's a **Swiss Army knife** of automation:

| Use Case | Example |
|----------|---------|
| **Configuration Management** | Install packages, configure files, manage services |
| **Application Deployment** | Deploy your app to 100 servers |
| **Orchestration** | Coordinate multi-step deployments across multiple tiers (web → app → db) |
| **Provisioning** | Create cloud resources (EC2 instances, S3 buckets, etc.) |
| **Security & Compliance** | Enforce security policies, patch servers |
| **Network Automation** | Configure routers, switches, firewalls (Cisco, Juniper, etc.) |
| **CI/CD** | Integrate with Jenkins, GitHub Actions, GitLab CI |
| **Cloud Management** | Manage AWS, Azure, GCP resources |
| **Container Management** | Build Docker images, manage Kubernetes resources |

---

## 🏛️ Ansible Architecture — How It Works Under the Hood

Ansible has a beautifully simple architecture. Let's break it down piece by piece.

### The Big Picture

```
    ┌─────────────────────────────────────────────────────────┐
    │                   CONTROL NODE                          │
    │              (Your laptop / CI server)                   │
    │                                                         │
    │   ┌─────────────┐  ┌─────────────┐  ┌──────────────┐  │
    │   │  Inventory   │  │  Playbook   │  │   Modules    │  │
    │   │ (which       │  │ (what to    │  │ (how to      │  │
    │   │  servers?)   │  │  do?)       │  │  do it?)     │  │
    │   └──────┬──────┘  └──────┬──────┘  └──────┬───────┘  │
    │          │                │                 │           │
    │          └────────────────┼─────────────────┘           │
    │                          │                              │
    │                    ┌─────▼─────┐                        │
    │                    │  Ansible   │                        │
    │                    │  Engine    │                        │
    │                    └─────┬─────┘                        │
    │                          │                              │
    └──────────────────────────┼──────────────────────────────┘
                               │
                          SSH / WinRM
                               │
              ┌────────────────┼────────────────┐
              │                │                │
              ▼                ▼                ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │  Managed      │  │  Managed      │  │  Managed      │
    │  Node 1       │  │  Node 2       │  │  Node 3       │
    │  (web server) │  │  (app server) │  │  (db server)  │
    │               │  │               │  │               │
    │  No agent     │  │  No agent     │  │  No agent     │
    │  installed!   │  │  installed!   │  │  installed!   │
    │  Just Python  │  │  Just Python  │  │  Just Python  │
    └──────────────┘  └──────────────┘  └──────────────┘
```

### Component Breakdown

Let's understand each component:

---

### 1. 🖥️ Control Node

This is the machine **where Ansible is installed** and from which you run all your automation. 

```
Control Node = The "brain" / "command center"

What it can be:
    ✅ Your laptop
    ✅ A dedicated management server
    ✅ A CI/CD server (Jenkins, GitHub Actions runner)
    ✅ Any Linux/macOS machine

What it CANNOT be:
    ❌ A Windows machine (you can't install Ansible control node on Windows natively)
    ✅ BUT you can use WSL2 on Windows (which runs Linux inside Windows)
    
Requirements:
    → Python 3.x installed
    → Ansible installed (pip install ansible)
    → SSH access to managed nodes
    → That's it!
```

**Key point:** Ansible is only installed on the Control Node. Not on the managed servers.

---

### 2. 🖥️ Managed Nodes (Target Hosts)

These are the servers you want to configure/manage. They're the "targets" of your automation.

```
Managed Nodes = The servers Ansible manages

Requirements:
    → SSH server running (Linux) or WinRM enabled (Windows)
    → Python installed (Python 2.7+ or Python 3.x) — most Linux distros have this by default
    → That's it! No Ansible agent, no special software!

These can be:
    ✅ Physical servers
    ✅ Virtual machines (EC2, Azure VM, GCE)
    ✅ Containers (though less common)
    ✅ Network devices (routers, switches)
    ✅ Cloud services
```

---

### 3. 📋 Inventory

The inventory is a file that lists all the servers Ansible should manage. Think of it as an **address book** for your servers.

```ini
# inventory.ini — Simple inventory file

# Individual servers
192.168.1.10
192.168.1.11
192.168.1.12

# Grouped servers
[webservers]
web1.example.com
web2.example.com
web3.example.com

[databases]
db1.example.com
db2.example.com

[monitoring]
grafana.example.com
prometheus.example.com
```

**Why groups?** Because you might want to apply different configurations to different types of servers:
- Web servers get Nginx
- Database servers get PostgreSQL
- Monitoring servers get Grafana

We'll dive deep into inventory in the next note (13.3).

---

### 4. 📝 Playbooks

Playbooks are **YAML files** that describe the desired state of your servers. They contain the "what to do" instructions.

```yaml
# setup-webserver.yml — A simple Ansible Playbook
---
- name: Set up web servers                # Description of this play
  hosts: webservers                        # Which servers to target (from inventory)
  become: yes                              # Run as root (sudo)

  tasks:                                   # List of things to do
    - name: Install Nginx                  # Description of this task
      apt:                                 # Use the "apt" module
        name: nginx                        # Package to install
        state: present                     # Make sure it's installed

    - name: Start Nginx                    # Description of this task
      service:                             # Use the "service" module
        name: nginx
        state: started                     # Make sure it's running
        enabled: yes                       # Make sure it starts on boot

    - name: Copy custom index page
      copy:                                # Use the "copy" module
        src: files/index.html              # Source file on control node
        dest: /var/www/html/index.html     # Destination on managed node
        owner: www-data
        group: www-data
        mode: '0644'
```

**What's happening here (line by line):**

| Line | What It Means |
|------|--------------|
| `---` | Start of a YAML document |
| `- name: Set up web servers` | A human-readable name for this "play" |
| `hosts: webservers` | Run these tasks on all servers in the `[webservers]` group from inventory |
| `become: yes` | Execute tasks with `sudo` (root privileges) |
| `tasks:` | Here comes the list of things to do |
| `- name: Install Nginx` | Human-readable label for this task |
| `apt:` | Use the `apt` module (for Debian/Ubuntu package management) |
| `name: nginx` | The package to install |
| `state: present` | Make sure it IS installed (if already installed, do nothing = idempotent!) |

---

### 5. 🧩 Modules

Modules are the **building blocks** of Ansible. Each module knows how to do ONE specific thing. Ansible has **thousands** of modules built-in.

```
Think of modules as specialized tools in a toolbox:
    🔧 apt module     → Install/remove packages on Debian/Ubuntu
    🔧 yum module     → Install/remove packages on RedHat/CentOS
    🔧 service module → Start/stop/restart services
    🔧 copy module    → Copy files from control node to managed nodes
    🔧 template module → Copy files with variables (Jinja2 templates)
    🔧 file module    → Create/delete files and directories
    🔧 user module    → Create/manage users
    🔧 group module   → Create/manage groups
    🔧 lineinfile     → Add/modify a line in a file
    🔧 command module → Run any shell command
    🔧 shell module   → Run shell commands (with pipes, redirects)
    🔧 git module     → Clone/pull git repositories
    🔧 docker_container → Manage Docker containers
    🔧 k8s module     → Manage Kubernetes resources
    🔧 aws_ec2 module → Manage AWS EC2 instances
    ... and 3000+ more!
```

Example using different modules:

```yaml
tasks:
  # Using the "apt" module
  - name: Install packages
    apt:
      name:
        - nginx
        - git
        - curl
      state: present

  # Using the "user" module
  - name: Create a user
    user:
      name: deploy
      shell: /bin/bash
      groups: sudo

  # Using the "copy" module
  - name: Copy a config file
    copy:
      src: files/nginx.conf
      dest: /etc/nginx/nginx.conf

  # Using the "service" module
  - name: Restart Nginx
    service:
      name: nginx
      state: restarted
```

---

### 6. 🔌 Plugins

Plugins extend Ansible's core functionality. They're different from modules:

```
Modules = Do things ON managed nodes (install packages, manage files)
Plugins = Extend Ansible's core behavior ON the control node

Types of plugins:
    → Connection plugins: HOW to connect (SSH, WinRM, Docker, local)
    → Callback plugins: WHAT to do with output (log to file, send to Slack)
    → Lookup plugins: WHERE to get data (files, environment variables, databases)
    → Filter plugins: HOW to transform data (Jinja2 filters)
    → Inventory plugins: HOW to discover managed nodes (AWS, Azure, GCP)
```

---

### 7. 🏗️ Roles

Roles are a way to **organize** your Ansible code into **reusable, shareable packages**. Think of them as "chapters" in a book.

```
Without roles (messy):
    one-giant-playbook.yml    ← 500 lines, does everything, hard to maintain

With roles (organized):
    roles/
    ├── nginx/                 ← Everything about Nginx
    │   ├── tasks/main.yml
    │   ├── handlers/main.yml
    │   ├── templates/nginx.conf.j2
    │   ├── files/index.html
    │   └── defaults/main.yml
    ├── postgresql/            ← Everything about PostgreSQL
    │   ├── tasks/main.yml
    │   ├── handlers/main.yml
    │   └── templates/pg_hba.conf.j2
    └── monitoring/            ← Everything about monitoring
        ├── tasks/main.yml
        └── templates/prometheus.yml.j2
```

We'll cover roles in detail in note 13.6.

---

### 8. 🌐 Ansible Galaxy

**Ansible Galaxy** is a public repository of community-created roles and collections. Think of it as the "npm" or "Docker Hub" of Ansible.

```bash
# Instead of writing a role from scratch, download one:
ansible-galaxy install geerlingguy.nginx
ansible-galaxy install geerlingguy.docker
ansible-galaxy install geerlingguy.postgresql

# Browse at: https://galaxy.ansible.com
```

---

## 🔑 The "Agentless" Architecture — Why It's a Big Deal

This is one of Ansible's **killer features**. Let's understand why.

### What "Agentless" Means

```
AGENT-BASED tools (Puppet, Chef):
    → You must install special software (an "agent") on EVERY managed server
    → The agent runs as a background service
    → It uses CPU, memory, and disk space
    → You must keep the agent updated
    → If the agent crashes, the server stops being managed
    → More software = more attack surface (security risk)

AGENTLESS (Ansible):
    → Nothing to install on managed servers
    → Ansible connects via SSH (which is ALREADY running on every Linux server)
    → Sends small Python scripts over SSH, executes them, gets results, deletes them
    → No background processes consuming resources
    → No agent to maintain, update, or troubleshoot
    → Less software = smaller attack surface
```

### How Does Ansible Work Without an Agent?

Here's what happens step by step when you run an Ansible playbook:

```
Step 1: You run "ansible-playbook setup.yml" on your control node

Step 2: Ansible reads the playbook (YAML file) and the inventory (server list)

Step 3: Ansible opens SSH connections to all target servers (in parallel!)

Step 4: For each task in the playbook:
    a. Ansible generates a small Python script on the control node
    b. It copies that script to the target server (via SSH/SCP)
       → Usually placed in a temporary directory: /tmp/.ansible/tmp/
    c. It executes the script on the target server (via SSH)
    d. The script runs, does its thing, and returns the result (as JSON)
    e. Ansible collects the result
    f. The temporary script is deleted from the target server

Step 5: Ansible shows you a summary:
    "Server 1: OK (2 changed, 1 ok)"
    "Server 2: OK (0 changed, 3 ok)"
    "Server 3: FAILED (1 unreachable)"

Step 6: Done! No trace left on the servers.
```

Let's visualize this:

```
                    Control Node
                    ┌─────────────────────┐
                    │  ansible-playbook    │
                    │  setup.yml           │
                    └─────────┬───────────┘
                              │
                    ┌─────────▼───────────┐
                    │ 1. Read playbook     │
                    │ 2. Read inventory    │
                    │ 3. Generate Python   │
                    │    scripts           │
                    └─────────┬───────────┘
                              │
                         SSH (parallel)
                    ┌─────────┼──────────┐
                    │         │          │
                    ▼         ▼          ▼
              ┌──────────┐ ┌──────────┐ ┌──────────┐
              │ Server 1 │ │ Server 2 │ │ Server 3 │
              │          │ │          │ │          │
              │ 4. Copy  │ │ 4. Copy  │ │ 4. Copy  │
              │  script  │ │  script  │ │  script  │
              │ 5. Run   │ │ 5. Run   │ │ 5. Run   │
              │  script  │ │  script  │ │  script  │
              │ 6. Return│ │ 6. Return│ │ 6. Return│
              │  result  │ │  result  │ │  result  │
              │ 7. Delete│ │ 7. Delete│ │ 7. Delete│
              │  script  │ │  script  │ │  script  │
              └──────────┘ └──────────┘ └──────────┘
```

### Requirements for This to Work

On the **Control Node** (where Ansible is installed):
```
✅ Python 3.x
✅ Ansible installed (pip install ansible)
✅ SSH client (built into Linux/macOS)
✅ SSH key or password for target servers
```

On the **Managed Nodes** (target servers):
```
✅ SSH server running (sshd) — almost always running by default
✅ Python installed (Python 2.7+ or 3.x) — almost always present by default
✅ That's literally it!
```

---

## 🔐 SSH-Based Communication — How Ansible Connects

Ansible uses SSH to communicate with managed nodes. Here's how to set it up:

### Method 1: SSH Key-Based Authentication (Recommended)

```bash
# Step 1: Generate an SSH key pair on the control node (if you don't have one)
ssh-keygen -t ed25519 -C "ansible-control"

# Step 2: Copy the public key to all managed nodes
ssh-copy-id user@server1.example.com
ssh-copy-id user@server2.example.com
ssh-copy-id user@server3.example.com

# Step 3: Test the connection (should connect without asking for password)
ssh user@server1.example.com "hostname"

# Step 4: Now Ansible can connect automatically!
ansible all -i inventory.ini -m ping
```

### Method 2: Password-Based Authentication (Not Recommended for Production)

```bash
# You can use passwords, but you'll need sshpass installed
sudo apt install sshpass

# Then tell Ansible to use a password:
ansible all -i inventory.ini -m ping --ask-pass

# Or put it in the inventory (NOT recommended — passwords in plain text!):
# [webservers]
# server1.example.com ansible_user=admin ansible_password=MyPassword123
```

### SSH Configuration in Ansible

You can control SSH behavior in the Ansible configuration file:

```ini
# ansible.cfg — Ansible configuration file

[defaults]
# Default inventory file
inventory = ./inventory.ini

# Default remote user
remote_user = ubuntu

# Don't check SSH host keys (useful for dynamic cloud environments)
host_key_checking = False

# SSH connection timeout
timeout = 30

# Number of parallel connections (how many servers at once)
forks = 20

[ssh_connection]
# Use SSH pipelining for speed (avoids creating temp files)
pipelining = True

# SSH arguments
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
```

**What these settings mean:**

| Setting | What It Does |
|---------|-------------|
| `remote_user = ubuntu` | Ansible will SSH as the `ubuntu` user by default |
| `host_key_checking = False` | Won't ask "Are you sure you want to connect?" for new servers |
| `timeout = 30` | Wait 30 seconds before giving up on a connection |
| `forks = 20` | Connect to 20 servers at the same time (parallel execution) |
| `pipelining = True` | Speed optimization — reduces the number of SSH operations |

---

## 🔄 Ansible Execution Flow — What Happens When You Run a Playbook

Let's trace through a complete example to see exactly what happens:

### The Playbook

```yaml
# install-nginx.yml
---
- name: Install and configure Nginx
  hosts: webservers
  become: yes

  tasks:
    - name: Install Nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Ensure Nginx is running
      service:
        name: nginx
        state: started
        enabled: yes
```

### The Inventory

```ini
# inventory.ini
[webservers]
192.168.1.10
192.168.1.11
192.168.1.12
```

### The Execution

```bash
$ ansible-playbook -i inventory.ini install-nginx.yml
```

### What Happens Step by Step

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: PARSE                                               │
│ Ansible reads install-nginx.yml and inventory.ini           │
│ → "I need to connect to 3 servers: .10, .11, .12"         │
│ → "I need to run 2 tasks on each"                          │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│ Step 2: GATHER FACTS                                        │
│ Ansible SSHs into all 3 servers and collects "facts":       │
│ → Operating system: Ubuntu 22.04                            │
│ → IP addresses: 192.168.1.10                               │
│ → CPU: 4 cores                                              │
│ → Memory: 8 GB                                              │
│ → Hostname: web1                                            │
│ (These facts can be used as variables in the playbook)      │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│ Step 3: TASK 1 — Install Nginx                              │
│ For each server (in parallel):                              │
│   a. Generate a Python script that runs "apt install nginx" │
│   b. Copy the script to the server via SSH                  │
│   c. Execute the script                                     │
│   d. Script checks: "Is nginx already installed?"           │
│      → If YES: Return "ok" (no change needed)              │
│      → If NO: Install nginx, return "changed"              │
│   e. Delete the script from the server                      │
│   f. Report result back to control node                     │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│ Step 4: TASK 2 — Ensure Nginx is running                    │
│ Same process:                                               │
│   → Check if nginx service is running                       │
│   → If already running: "ok"                               │
│   → If not running: Start it, return "changed"             │
│   → Also enable it to start on boot                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│ Step 5: REPORT                                              │
│                                                             │
│ PLAY RECAP                                                  │
│ 192.168.1.10  : ok=3  changed=2  unreachable=0  failed=0  │
│ 192.168.1.11  : ok=3  changed=2  unreachable=0  failed=0  │
│ 192.168.1.12  : ok=3  changed=0  unreachable=0  failed=0  │
│                                                             │
│ (Server .12 already had nginx — nothing changed)           │
└─────────────────────────────────────────────────────────────┘
```

**Understanding the PLAY RECAP:**

| Status | Meaning |
|--------|---------|
| `ok=3` | 3 tasks ran successfully (including fact gathering) |
| `changed=2` | 2 tasks actually made changes on the server |
| `changed=0` | Server was already in the desired state (idempotent!) |
| `unreachable=0` | All servers were reachable via SSH |
| `failed=0` | No tasks failed |

---

## 📂 Ansible Project Structure — How Files Are Organized

Here's a typical Ansible project directory:

```
my-ansible-project/
├── ansible.cfg                  # Ansible configuration
├── inventory/
│   ├── production.ini           # Production servers
│   ├── staging.ini              # Staging servers
│   └── development.ini          # Dev servers
├── playbooks/
│   ├── site.yml                 # Main playbook (runs everything)
│   ├── webservers.yml           # Playbook for web servers
│   ├── databases.yml            # Playbook for database servers
│   └── monitoring.yml           # Playbook for monitoring servers
├── roles/
│   ├── nginx/                   # Nginx role
│   │   ├── tasks/main.yml
│   │   ├── handlers/main.yml
│   │   ├── templates/
│   │   ├── files/
│   │   ├── vars/main.yml
│   │   └── defaults/main.yml
│   ├── postgresql/              # PostgreSQL role
│   │   ├── tasks/main.yml
│   │   └── ...
│   └── common/                  # Common setup (applied to ALL servers)
│       ├── tasks/main.yml
│       └── ...
├── group_vars/
│   ├── all.yml                  # Variables for ALL servers
│   ├── webservers.yml           # Variables for webservers group
│   └── databases.yml            # Variables for databases group
├── host_vars/
│   ├── web1.example.com.yml     # Variables for a specific host
│   └── db1.example.com.yml
└── files/
    ├── nginx.conf               # Static files to copy
    └── ssl-cert.pem
```

**Don't worry about memorizing this!** We'll build this step by step in later notes. For now, just understand that Ansible projects have a clear, organized structure.

---

## 🆚 Ansible vs Other CM Tools — Why Ansible Wins

### Ansible vs Puppet

| Aspect | Ansible | Puppet |
|--------|---------|--------|
| **Architecture** | Agentless (SSH) | Agent-based (Puppet Agent) |
| **Language** | YAML (everyone knows it) | Puppet DSL (need to learn it) |
| **Setup time** | 5 minutes | Hours (master + agents) |
| **Learning curve** | Low | Medium-High |
| **Model** | Push (on-demand) | Pull (scheduled) |
| **Self-healing** | No (manual re-run) | Yes (automatic) |
| **Community** | 🟢 Massive | 🟡 Large but shrinking |
| **Best for** | General automation, cloud, DevOps | Enterprise compliance |

### Ansible vs Chef

| Aspect | Ansible | Chef |
|--------|---------|------|
| **Architecture** | Agentless | Agent-based (Chef Client) |
| **Language** | YAML | Ruby |
| **Learning curve** | Low | High (need to know Ruby) |
| **Setup** | Simple | Complex (Chef Server + Workstation + Clients) |
| **Community** | 🟢 Growing | 🔴 Shrinking |
| **Best for** | General use | Complex enterprise infra |

### Ansible vs SaltStack

| Aspect | Ansible | SaltStack |
|--------|---------|-----------|
| **Architecture** | Agentless | Agent (minion) or agentless |
| **Speed** | Good | Very fast (ZeroMQ) |
| **Language** | YAML | YAML |
| **Complexity** | Simple | Medium |
| **Community** | 🟢 Huge | 🟡 Medium |
| **Best for** | General use | High-speed, large scale |

### The Verdict

```
If you're starting fresh and need ONE configuration management tool:
    → Choose Ansible ✅

Why?
    → Simplest to learn (YAML — you probably already know it)
    → Agentless (nothing to install on servers)
    → Massive community and module ecosystem
    → Works for everything (config management, deployment, orchestration, cloud)
    → Red Hat backing = long-term support
    → Most demanded skill in job listings
```

---

## 🧠 Key Terminology Recap

| Term | Meaning |
|------|---------|
| **Control Node** | The machine where Ansible is installed (your laptop, CI server) |
| **Managed Node** | A server that Ansible manages (the target) |
| **Inventory** | A file listing all managed nodes (the "address book") |
| **Playbook** | A YAML file with automation instructions (the "recipe") |
| **Play** | A section of a playbook targeting specific hosts |
| **Task** | A single action in a playbook (install something, copy a file, etc.) |
| **Module** | A built-in tool that does one specific thing (apt, copy, service, etc.) |
| **Role** | A reusable, organized package of tasks, files, templates, and variables |
| **Facts** | Auto-collected info about managed nodes (OS, IP, CPU, RAM, etc.) |
| **Handler** | A task that only runs when triggered (e.g., restart service after config change) |
| **Ansible Galaxy** | Public repository of community roles and collections |
| **Collection** | A package format for distributing Ansible content (roles, modules, plugins) |
| **Idempotent** | Running the same task multiple times = same result, no side effects |

---

## 📝 Summary

| Concept | Key Takeaway |
|---------|-------------|
| **What is Ansible?** | Open-source, agentless automation tool by Red Hat, written in Python |
| **What it does** | Configuration management, app deployment, orchestration, provisioning |
| **Architecture** | Control Node → SSH → Managed Nodes (no agent needed) |
| **Language** | YAML (Playbooks) — simple, human-readable |
| **How it works** | Generates Python scripts, copies them via SSH, executes, returns results, cleans up |
| **Key advantage** | Agentless — just need SSH and Python on target servers |
| **Modules** | 3000+ built-in modules for every task imaginable |
| **Why choose Ansible** | Simplest, most popular, largest community, works for everything |

---

## ⏭️ What's Next?

Now that you understand Ansible's architecture and how it works, let's get our hands dirty! In the next note, we'll install Ansible and set up our inventory.

**Next up: 13.3 — Ansible Installation & Inventory**
